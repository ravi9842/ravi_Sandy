<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>My Valentine</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <style>
    :root {
      --bg: #050816;
      --card: #111827;
      --accent: #ec4899;
      --accent-soft: rgba(236, 72, 153, 0.2);
      --text-main: #f9fafb;
      --text-soft: #9ca3af;
      --highlight: #fbbf24;
      --shadow-strong: 0 20px 40px rgba(0, 0, 0, 0.7);
      --radius-xl: 24px;
      --radius-lg: 18px;
      --radius-md: 12px;
      --glass: rgba(15, 23, 42, 0.85);
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      background: radial-gradient(circle at top, #1f2937 0, #020617 45%, #000 100%);
      color: var(--text-main);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
      overflow-x: hidden;
    }

    .valentine-shell {
      position: relative;
      max-width: 1100px;
      width: 100%;
      background: radial-gradient(circle at top left, #1f2937 0, #020617 55%);
      border-radius: 32px;
      padding: 26px;
      box-shadow: var(--shadow-strong);
      overflow: hidden;
      border: 1px solid rgba(148, 163, 184, 0.25);
    }

    /* Floating background hearts */
    .floating-heart {
      position: absolute;
      font-size: 40px;
      opacity: 0.12;
      animation: float 16s linear infinite;
      pointer-events: none;
    }

    .floating-heart:nth-child(1) { top: 8%; left: 6%; animation-delay: 0s; }
    .floating-heart:nth-child(2) { top: 18%; right: 10%; animation-delay: 3s; }
    .floating-heart:nth-child(3) { bottom: 10%; left: 18%; animation-delay: 6s; }
    .floating-heart:nth-child(4) { bottom: 4%; right: 4%; animation-delay: 9s; }

    @keyframes float {
      0% { transform: translateY(0) translateX(0) scale(1); }
      50% { transform: translateY(-20px) translateX(10px) scale(1.1); }
      100% { transform: translateY(0) translateX(0) scale(1); }
    }

    .grid {
      display: grid;
      grid-template-columns: minmax(0, 1.2fr) minmax(0, 1fr);
      gap: 24px;
    }

    @media (max-width: 900px) {
      .grid {
        grid-template-columns: 1fr;
      }
    }

    /* Left: main love card */
    .love-card {
      background: linear-gradient(135deg, rgba(15, 23, 42, 0.95), rgba(15, 23, 42, 0.7));
      border-radius: var(--radius-xl);
      padding: 24px 22px 22px;
      position: relative;
      overflow: hidden;
      border: 1px solid rgba(148, 163, 184, 0.35);
    }

    .love-card::before {
